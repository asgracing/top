# Install under /etc/nginx/conf.d/; limit_req_zone is valid only in http context.
limit_req_zone $binary_remote_addr zone=asg_donations:1m rate=30r/m;
limit_req_zone $binary_remote_addr zone=asg_donations_oauth:1m rate=6r/m;
